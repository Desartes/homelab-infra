k3s_cluster:
  children:
    server:
      hosts:
        192.168.1.20:
    agent:
      hosts:
  vars:
    ansible_user: ubuntu
    k3s_version: v1.31.12+k3s1
    api_endpoint: "{{ hostvars[groups['server'][0]]['ansible_host'] | default(groups['server'][0]) }}"